---
get:
  tags:
    - users
  summary: Get user
  responses:
    "200":
      description: Ok
      content:
        application/json:
          schema:
            $ref: ../../schemas/MyResponse.yaml
    "404":
      description: not found
      content:
        application/json:
          schema:
            $ref: ../../schemas/MyResponse.yaml
