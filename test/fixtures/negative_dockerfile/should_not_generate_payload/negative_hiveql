# HiveQL - FROM before SELECT is valid syntax
FROM table_name
# "_" is not allowed outside of image tag
SELECT col1, col2, col3
WHERE partition_date = '2024-01-01'

# HiveQL with database-qualified table
FROM database.table_name
SELECT *
LIMIT 100