openapi: 3.0.2
info:
  title: ICE Results API
  description: TBD
  version: 1.0.0
tags:
  - name: Results
paths:
  '/results/{scan-id}':
    get:
      tags:
        - Results
      summary: get result by scan id
      description: Returns result item for specific scan
      operationId: findResultsByScanID
      parameters:
        - in: path
          name: scan-id
          schema:
            type: string
          required: true
          description: get by Scan ID field
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ResultItem'
        '500':
          description: Internal Server Error.
components:
  schemas:
    ResultItem:
      type: object
      properties:
        ID:
          type: string
        fileName:
          type: string
        line:
          type: integer
        queryName:
          type: string
          description: Query name
        query_id:
          type: string
        search_key:
          type: string
          description: DOM path in source file
        key_expected_value:
          type: string
        key_actual_value:
          type: string
        value:
          type: string
        severity:
          $ref: '#/components/schemas/SeverityEnum'
        issue_type:
          $ref: '#/components/schemas/IssueTypeEnum'
    IssueTypeEnum:
      type: string
      description: Issue type enum of a result.
      enum:
        - MissingAttribute
        - RedundantAttribute
        - IncorrectValue
    SeverityEnum:
      type: string
      description: Severity enum of a result.
      enum:
        - HIGH
        - MEDIUM
        - LOW
        - INFO
  securitySchemes:
    accessControl:
      type: http
      scheme: bearer
      bearerFormat: JWT