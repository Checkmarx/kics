swagger: "2.0"
info:
  title: Tariff Setup API
  version: 1.0.0

paths:
  /CreateTariffSetup:
    post:
      summary: Creates tariff setups.
      tags:
        - Tariff Setups
      produces:
        - application/json
      responses:
        400:
          $ref: "#/responses/missing_component" # incorrect referencing
        401:
          $ref: "#/responses/ErrorResponse"     # responses/ErrorResponse is missing a schema field

responses:
  ErrorResponse:
    description: Error response
