version: '2.1'

services:
 lelele-service:
    build: lelele-apigateway/
    image: lelele-service
    restart: always
    container_name: lelele
    network_mode: "host"
    hostname: localhost
    ports:
      - 8092:8092
    healthcheck:
      test: "exit 0"
