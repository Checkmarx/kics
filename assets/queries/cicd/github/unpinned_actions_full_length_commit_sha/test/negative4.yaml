name: test-negative4
on:
  pull_request:
    types: [opened, synchronize, edited, reopened]
    branches:
      - master
jobs:
  test-negative4:
    runs-on: ubuntu-latest
    steps:
    - name: Local action (parent folder)
      uses: ../test.yml
