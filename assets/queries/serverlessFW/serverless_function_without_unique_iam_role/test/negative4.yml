service: service-single-function
frameworkVersion: '2'
provider:
  name: aws
  runtime: nodejs12.x

# Single function should NOT be flagged - can use default AWS execution roles
functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /hello
          method: get
